--- src/Plugin/EntityBrowser/FieldWidgetDisplay/RenderedEntity.php
+++ src/Plugin/EntityBrowser/FieldWidgetDisplay/RenderedEntity.php
@@ -73,7 +74,11 @@ class RenderedEntity extends FieldWidgetDisplayBase implements ContainerFactoryP
    * {@inheritdoc}
    */
   public function view(EntityInterface $entity) {
-    return $this->entityTypeManager->getViewBuilder($this->configuration['entity_type'])
+
+    if (empty($this->configuration['view_mode'])) {
+      return ['#markup' => $entity->label()];
+    }
+    return $this->entityTypeManager->getViewBuilder($entity->getEntityTypeId())
       ->view($entity, $this->configuration['view_mode']);
   }
 
